[project]
name = "workpulse-checker"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "mss>=10.2.0",
    "pandas>=3.0.5",
    "psutil>=7.2.2",
    "pyarrow>=25.0.1",
    "pywin32>=312",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
